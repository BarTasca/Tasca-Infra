version: "3.9"

services:
  api:
    volumes:
      - ../BarTascaBackend:/src
    working_dir: /src/BarTascaBackend
    command: ["dotnet", "watch", "run", "--no-launch-profile", "--urls", "http://0.0.0.0:8080"]
